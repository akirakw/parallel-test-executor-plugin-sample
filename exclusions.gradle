allprojects {
    plugins.withId('java') {
        def f = file('exclusions.txt')
        if (f.exists()) {
            test {
                exclude f.readLines('UTF-8')
                ignoreFailures = true
            }
        }
    }
}
